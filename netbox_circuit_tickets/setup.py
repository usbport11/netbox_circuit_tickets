from setuptools import find_packages, setup

setup(
    name='netbox_circuit_tickets',
    version='0.1',
    description='Manage curcuit service tickets',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)

